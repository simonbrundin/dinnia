const router = useRouter();

export const signUp = async (email: string) => {
  console.log("Start signUp");

  // const { error, session } = await nhost.auth.signUp({
  //   email,
  //   securityKey: true,
  // });
  // console.log("signUpError", error);
  // console.log("signUpSession", session);

  // if (error) {
  //   return error;
  // }
  // if (session) {
  //   navigateTo("/");
  // }
};
